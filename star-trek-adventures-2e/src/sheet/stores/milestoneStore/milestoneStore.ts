import { defineStore } from "pinia";

export const useMilestoneStore = defineStore("milestones", () => {

  const hydrate = () => { };
  const dehydrate = () => { }
  return {
    hydrate,
    dehydrate
  }
})