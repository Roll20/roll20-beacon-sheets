{{#> postWrapper type="roll" characterName=characterName bottomBarValues=bottomBarValues}}
  <div class="roll">
    <ul class="roll__dice-results">
      {{#each dice}}
        <li>{{this}}</li>
      {{/each}}
    </ul>
  </div>
{{/ postWrapper }}