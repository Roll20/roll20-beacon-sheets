Rolling {{#each components}}
          {{#if dice}}{{count}}d{{sides}} (
            {{#each dice}}
                {{this}}
                {{#unless @last}}+{{/unless}}
            {{/each}}
          ){{/if}}
        {{/each}} 
    {{#each components}}
      {{#unless dice}}
        {{#if value}} 
          {{#if (isGreater value 0)}}+{{/if}} 
          {{value}}  
        {{/if}}
      {{/unless}}
    {{/each}}
    {{#each components}}
      {{#if (isEqual trained false)}} / 2{{/if}} 
      {{#unless dice}}
        = {{sumComponents ../components multiplier}}
      {{/unless}}
{{/each}}
{{!-- Rolling {{#each components}}
  {{#if dice}}
    {{count}}d{{sides}} (
    {{#each dice}}
      <span style="{{#if (isEqual this 1)}}color:red{{/if}} {{#if (isEqual this ../sides)}}color:green{{/if}}">
        {{this}}
      </span>{{#unless @last}}+{{/unless}}
    {{/each}})
  {{/if}}
{{/each}} 
{{#each components}}
  {{#unless dice}}{{#if value}} + {{value}}{{/if}}{{/unless}}
{{/each}} --}}
